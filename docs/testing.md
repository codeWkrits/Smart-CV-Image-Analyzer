# Testing

Run:

```bash
pytest -q
```

The suite covers image statistics, edge preprocessing, output creation, and triangle contour classification.

For final demonstration, test with at least one readable image and one short video and verify that the annotated outputs are created.
